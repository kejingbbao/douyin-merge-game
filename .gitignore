# 依赖
node_modules/

# 本地环境变量 / 密钥
.env
.env.*
!.env.example

# 真实配置（含 RANK_SECRET 等签名密钥，绝不入仓）
# 部署 / clone 后请复制 config.example.js 为 config.js 再填真实值
config.js

# 日志 & 调试产物
*.log
*.local
coverage/

# OS / 编辑器
.DS_Store
Thumbs.db
*.swp
*.swo
